name: Automated SEO & API Health Pipeline

on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  run-automation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install google-api-python-client google-auth requests google-generativeai

      - name: Run API Health Check
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          BING_API_KEY: ${{ secrets.BING_API_KEY }}
        run: python health_check.py

      - name: Run Main GSC Report Automation
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: python gsc_report.py

      - name: Commit and Push Generated Reports
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "action@github.com"
          git add .
          git diff --quiet && git diff --cached --quiet || (git commit -m "chore: auto-update reports and data [skip ci]" && git push)

      - name: Generate Sitemap
        run: python generate_sitemap.py

      - name: Ping Search Engines
        run: |
          curl -s "https://www.google.com/ping?sitemap=https://www.uonogamesapk.com/sitemap.xml"
          curl -s "https://www.bing.com/ping?sitemap=https://www.uonogamesapk.com/sitemap.xml"

      - name: Commit and Push Generated Reports
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "action@github.com"
          git add sitemap.xml
          git diff --quiet && git diff --cached --quiet || (git commit -m "chore: auto-update sitemap [skip ci]" && git push)
